# Default GitHub Templates

This repository provides default GitHub Issue and Pull Request templates for all repositories in this account/organization.

Files here are used by GitHub as fallback templates when a repository does not define its own local templates.
